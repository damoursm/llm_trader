"""Plotly Dash monitoring dashboard.

Three tabs over the DuckDB store:
  * Recommendations & Rationale — every call + the APIs used that run.
  * Entry Performance           — per-method solo win rates and stats.
  * Returns                     — headline P&L, equity curve, open/closed trades.

Launch with ``python main.py --dashboard``.
"""
